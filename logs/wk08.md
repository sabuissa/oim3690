# Week 8 Log

## What I built
- playlist.html + js/playlist.js — render-from-data: an array of songs, a
  for...of loop that creates one <li> per song, and an if that marks any
  title containing "Remix". Then extended with AI to add an "Add song" input.
- fetch-fun.html — an async function that fetches a joke from the JokeAPI,
  reads data.joke, and shows it, wrapped in try/catch. Extended with a
  "Surprise me" button that switches between two APIs.
- post-message.html — an async POST to the class API with my X-Token.
- ai-chat/ folder — index.html, script.js (an askAI helper that POSTs to the
  OpenAI API and reads data.choices[0].message.content), and config.js for
  the key. Added config.js to .gitignore and committed that first, then
  confirmed with git status that config.js is never tracked.

## What I learned
- The biggest one is .gitnore.
- How to integrate AI and how to keep it safe.

## Why the .gitignore step mattered
- config.js contains a password pretty much, so since this website is public and anyone can access it, .gitnore makes sure that git never tracks it or commits it so it stays local to this laptop and cannot be scanned.

## An AI suggestion I did NOT accept right away
- AI started asking to commit things that was python to my MP3 which is code I am not familiar with and not aligned with the class so I asked claude chat what it could be and it turns out to be just an internal check (probably) becuase the final code had no python.