# Week 7 Log

## What we covered
- Arrays: storing many values in one variable and reading them by index
  (index starts at 0 — the first item is position 0, not 1).
- Three ways to loop: the classic `for` (best when you need the index),
  `for...of` (cleanest when you just want each value), and `forEach`
  (gives you both item and index).
- The core pattern behind real apps: take an array, loop over it, add an
  `if` — that's the engine behind a feed, a search, or a gallery.
- Saw an infinite loop freeze the browser, and why a loop's start/stop/step
  all have to be right.
- Started the MP2 Showcase and launched MP3 (the API-powered app).

## What I built this week
- Started MP3: created the repo and began the PROPOSAL.md.

## What I learned
- Starting to understand arrays and loops — specifically beginning to get a
  sense of how and when to use an array together with a loop.